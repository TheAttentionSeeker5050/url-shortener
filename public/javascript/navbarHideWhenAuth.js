// This will hide the log in and register elements when the user is authenticated
const loginElement = document.getElementById("loginElement")
const registerElement = document.getElementById("registerElement")

loginElement.classList.add("hidden")
registerElement.classList.add("hidden")
